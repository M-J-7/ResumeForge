/**
 * The product's name, in one place.
 *
 * §12 Q3 is still open — the name has not been chosen. Everything that shows
 * a name to a user reads it from here, so answering that question is a
 * one-line change rather than a search-and-replace across pages, emails, and
 * PDF metadata.
 */
export const PRODUCT_NAME = "ATS Resume Builder";
